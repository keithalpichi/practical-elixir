# Implement the HelloWorld module and `greet/1` function.
# The `/n` after a function where n is a number describes the arity (or number of arguments) that function accepts. the `greet/1` function takes one argument.
#
# http://elixir-lang.org/docs/stable/elixir/Kernel.html#defmodule/2
# http://elixir-lang.org/getting-started/modules.html#default-arguments
#
# Functions should be in "snake" case.
#
# Consider adding a `moduledoc`, `doc` and other module attributes: http://elixir-lang.org/docs/stable/elixir/writing-documentation.html
