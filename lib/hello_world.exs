# Learning Outcomes:
# - use of modules and functions
# - use of default arguments to functions
# - use of module documentation

# Restrictions: none

# Resources:
# - http://elixir-lang.org/docs/stable/elixir/Kernel.html#defmodule/2
# - http://elixir-lang.org/getting-started/modules.html#default-arguments

# Instructions:
# - Implement the HelloWorld module and `greet/1` function. The `/n` after a function where n is a number describes the arity (or number of arguments) that function accepts. the `greet/1` function takes one argument.
# - Modules should be in "camel" case, functions "snake" case
# - Document your module and functions with `moduledoc`, `doc` and other module attributes: http://elixir-lang.org/docs/stable/elixir/writing-documentation.html
